# Injective-native signal threshold specification

Binary badge is earned only if all thresholds are satisfied:

- NFTs minted >= 3
- Unique dApps interacted >= 3
- Total transactions >= 25
- Days active >= 14

Tooltip text:

> This badge indicates verifiable on-chain contribution to the Injective ecosystem based on transaction history, NFT creation, and dApp interaction thresholds.
